# Block direct public access — videos stream only via training/stream.php
Options -Indexes
Require all denied

# Apache 2.2 fallback
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
