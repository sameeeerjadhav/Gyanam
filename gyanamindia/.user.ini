; Hostinger PHP-FPM settings (takes effect within a few minutes after upload)
; Keep in sync with .htaccess — required for large training video uploads
upload_max_filesize = 1024M
post_max_size = 1100M
max_execution_time = 3600
max_input_time = 3600
memory_limit = 512M
file_uploads = On
